buildscript {
    apply from: 'gradle/buildscript.gradle', to: buildscript
}

apply plugin: 'org.sonarqube'
apply plugin: 'jacoco'

jacoco {
    toolVersion = '0.7.9'
}

jacocoTestReport {
    reports {
        xml.enabled true
    }
}